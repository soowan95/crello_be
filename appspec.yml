version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/crello/be
permissions:
  - object: /home/ubuntu/crello/be
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu